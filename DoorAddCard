
/*
PINOUT:
RC522 MODULE    Uno/Nano     MEGA
SDA             D10          D9
SCK             D13          D52
MOSI            D11          D51
MISO            D12          D50
IRQ             N/A          N/A
GND             GND          GND
RST             D9           D8
3.3V            3.3V         3.3V
*/
/* Include the standard Arduino SPI library */
#include <SPI.h>
#include<Servo.h>
/* Include the RFID library */
#include <RFID.h>

/* Define the DIO used for the SDA (SS) and RST (reset) pins. */
#define SDA_DIO 9
#define RESET_DIO 8
#define serialnumbernumber 25
/* Create an instance of the RFID library */
RFID RC522(SDA_DIO, RESET_DIO); 
Servo servo;
typedef struct { 
  char* Name = (char*)malloc(sizeof(char) * 25);
  char* serial_number = (char*)malloc(sizeof(char) * 5);
} cardDictionary;
cardDictionary myDictionary[10];
cardDictionary* myDictionaryPt = &myDictionary[0];
int numberofkeys = 1;
int numloop2 = 0;

void setup()
{ 
  Serial.begin(9600);
  /* Enable the SPI interface */
  SPI.begin(); 
  /* Initialise the RFID reader */
  RC522.init();
  RC522.antennaOn();
Serial.println("Please Scan a Registered KeyCard");
  servo.attach(7);
  myDictionaryPt->Name = "key 1";
  myDictionaryPt->serial_number="4861425120";
Serial.println(myDictionaryPt->Name);
}

void loop()
{
//if keycheck = true
if (RC522.isCard()){
  Serial.println("tp2");
  if(RC522.readCardSerial()){
    Serial.println("tp3");
    boolean isregistered = true;
    char * temp2 = (char)malloc(sizeof(char)*5);
    temp2 = 0;
    long temp3 = 0;
    for(int i  = 0; i < 5; i++){
      temp3 += 
      Serial.print("Actual: ");
      Serial.println(RC522.serNum[i]);
      temp3 = (RC522.serNum[i]);
      Serial.print("Temp 3: ");
      Serial.println(temp3);
    }
    Serial.println("tp4");
    Serial.println(temp3);
    delay(1000);
   
  
  
 /* if (card == (myDictionaryPt+i)->serial_number){
    Serial.println("2");
    char* newcard = (char*)malloc(sizeof(char)*15);
    newcard = (char*)RC522.readCardSerial();
    Serial.println(strcmp(card, newcard));
    numberofkeys ++;
    Serial.println("a");
    if (numloop2==0){
    
    while (strcmp(card, newcard));
    Serial.println("Please Scan the Card You Want to Add");
    RC522.readCardSerial();
    numloop2++;
    Serial.println("3");
    }
    RC522.readCardSerial();
    Serial.println(card);
    Serial.println("4");
    for(int i=0;i<5;i++)
    {
      card+=RC522.serNum[i];
    }
    myDictionaryPt->serial_number = card;
    //enter the name of new key, "x"
    //(myDictionary + numberofkeys)->NAME=x;
    
  }

  */
  }
  
  }
  
}

